/* Phase 1 stub — admin event management. */
export default function AdminEventsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-navy mb-6">Events (Admin)</h1>
      <p className="text-text-muted">Event creation, editing, and check-in QR codes coming in Phase 1.</p>
    </div>
  );
}

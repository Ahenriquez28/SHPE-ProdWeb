/* Phase 1 stub — member directory (requires shareContact = true). */
export default function DirectoryPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-navy mb-6">Member Directory</h1>
      <p className="text-text-muted">Directory search and cards coming in Phase 1.</p>
    </div>
  );
}
